---
import { tag } from "@/lib/tera";

export interface Props {
    expression: string;
    debugEval?: string;
}

const { debugEval, expression } = Astro.props as Props;
---

{
    import.meta.env.PROD ? (
        <Fragment set:html={tag(expression)} />
    ) : (
        <Fragment set:html={debugEval ?? expression} />
    )
}
